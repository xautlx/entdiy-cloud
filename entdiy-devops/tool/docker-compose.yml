version: '3'
services:
  docker_registery:
    image: registry:2
    user: root
    ports:
      - 5000:5000
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - /usr/bin/docker:/usr/bin/docker
      - ${BASE_DIR}/data/docker-registery:/var/lib/registry
    privileged: true
    restart: always
    hostname: docker_registery
  jenkins:
    image: jenkins/jenkins:jdk11
    user: root
    ports:
      - 9000:8080
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - /usr/bin/docker:/usr/bin/docker
      - ${BASE_DIR}/data/jenkins:/var/jenkins_home
    environment:
      - JENKINS_OPTS="--prefix=/jenkins"
    privileged: true
    restart: always
    hostname: jenkins
    links:
      - docker_registery